'use strict';
const fs=require('fs'),path=require('path'),crypto=require('crypto');
const root=path.resolve(__dirname,'..'),hash=b=>crypto.createHash('sha256').update(b).digest('hex');
const required=['index.html','README.md','LICENSE','CITATION.cff','.zenodo.json','package.json','pnpm-lock.yaml','provenance.json','docs/SUBMISSION-REVIEW.md','docs/SCIENTIFIC-STATUS.md','tests/run-audit.cjs','.github/workflows/ci.yml'];
for(const f of required)if(!fs.existsSync(path.join(root,f)))throw Error('Missing required file: '+f);
const provenance=JSON.parse(fs.readFileSync(path.join(root,'provenance.json'),'utf8'));
const html=fs.readFileSync(path.join(root,'index.html'),'utf8'),htmlHash=hash(html);
if(htmlHash!==provenance.runtimeSha256)throw Error('HTML differs from reviewed baseline; create and validate a new release intentionally');
const scripts=[...html.matchAll(/<script\b([^>]*)>([\s\S]*?)<\/script>/gi)].filter(m=>!m[1].includes('data-aghip-vendor'));
const declared=html.match(/const SCIENTIFIC_BUNDLE_SHA256 = '([a-f0-9]{64})';/)?.[1];
const canonical=scripts.map(m=>m[2].replace(/const SCIENTIFIC_BUNDLE_SHA256 = '[a-f0-9]{64}';/,"const SCIENTIFIC_BUNDLE_SHA256 = '"+'0'.repeat(64)+"';")).join('\n');
if(!declared||hash(canonical)!==declared)throw Error('Application-script digest mismatch');
for(const entry of provenance.preservedFiles){const b=fs.readFileSync(path.join(root,entry.path));if(hash(b)!==entry.sha256)throw Error('Preserved evidence changed: '+entry.path);}
const summary=JSON.parse(fs.readFileSync(path.join(root,'reports/r11/audit/audit-summary.json'),'utf8'));
if(summary.htmlSha256!==htmlHash||summary.summary.failedSuites!==0||summary.summary.passedChecks!==1717)throw Error('Historical baseline mismatch');
const workflow=fs.readFileSync(path.join(root,'.github/workflows/ci.yml'),'utf8');
const YAML=require('yaml');
const ci=YAML.parse(workflow),citation=YAML.parse(fs.readFileSync(path.join(root,'CITATION.cff'),'utf8'));
YAML.parse(fs.readFileSync(path.join(root,'.github/dependabot.yml'),'utf8'));
YAML.parse(fs.readFileSync(path.join(root,'pnpm-lock.yaml'),'utf8'));
if(!ci.on?.pull_request&&ci.on?.pull_request!==null)throw Error('Missing pull-request trigger');
if(ci.permissions?.contents!=='read'||!ci.jobs?.audit?.steps?.length)throw Error('Invalid CI structure');
if(citation['cff-version']!=='1.2.0'||!citation.title||!citation.authors?.length)throw Error('Incomplete citation metadata');
for(const match of workflow.matchAll(/uses:\s+([^\s#]+)/g))if(!/@[a-f0-9]{40}$/.test(match[1]))throw Error('Unpinned action: '+match[1]);
if(workflow.includes('pull_request_target:')||!workflow.includes('contents: read'))throw Error('Unexpected CI privilege');
console.log(JSON.stringify({status:'PASS',htmlSha256:htmlHash,preservedFiles:provenance.preservedFiles.length,scope:'Repository identity and configuration checks, not scientific validation'},null,2));
